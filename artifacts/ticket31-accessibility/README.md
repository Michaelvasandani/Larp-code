# Ticket 31 packaged accessibility evidence

Run `pnpm build && pnpm accessibility:evidence` to refresh this evidence from the packaged extension. The harness records the representative signed-out journey at `380 × 600`, keyboard reachability, reduced-motion behavior, and the `200%` text-scale journey at `800 × 600`. The state manifest keeps the reviewed signed-out, setup, Invitation, Scheduled, balanced/behind Active, unavailable, update-required, Success, Incomplete, Canceled, and Abandoned acceptance coverage visible alongside the screenshots.
